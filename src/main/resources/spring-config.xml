<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/tx
           https://www.springframework.org/schema/tx/spring-tx.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd">

    <!--
        DATASOURCE
        Conexion a H2 en memoria. Para cambiar a MySQL/Oracle
        solo cambias driverClassName, url, username y password.
    -->
    <bean id="dataSource"
          class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="org.h2.Driver"/>
        <property name="url"
                  value="jdbc:h2:mem:cruddb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE"/>
        <property name="username" value="sa"/>
        <property name="password" value=""/>
    </bean>

    <!--
        SESSION FACTORY DE HIBERNATE
        LocalSessionFactoryBean es el puente entre Spring e Hibernate.
        Le decimos:
          - que DataSource usar
          - donde estan los archivos .hbm.xml de mapeo
          - propiedades propias de Hibernate
    -->
    <bean id="sessionFactory"
          class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">

        <property name="dataSource" ref="dataSource"/>

        <property name="mappingResources">
            <list>
                <value>Estado.hbm.xml</value>
                <value>Cliente.hbm.xml</value>
            </list>
        </property>

        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">
                    org.hibernate.dialect.H2Dialect
                </prop>
                <!-- create-drop: crea tablas al iniciar, las elimina al cerrar -->
                <prop key="hibernate.hbm2ddl.auto">create-drop</prop>
                <!-- Ver el SQL generado en consola -->
                <prop key="hibernate.show_sql">true</prop>
                <prop key="hibernate.format_sql">true</prop>
            </props>
        </property>
    </bean>

    <!--
        TRANSACTION MANAGER
        Gestiona las transacciones usando la SessionFactory de Hibernate.

    -->
    <bean id="transactionManager"
          class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <!-- Activa soporte de @Transactional en los beans -->
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- Spring escanea los @Service, @Repository, @RestController -->
    <context:component-scan base-package="com.prueba.crud"/>

</beans>